// app/member/rewards/page.tsx
export default function MemberRewardsPage() {
  return (
    <main className="mx-auto max-w-5xl px-4 py-6">
      <h1 className="text-2xl font-semibold">Member Rewards</h1>
      <p className="text-neutral-400 mt-2">
        Placeholder — we can migrate the current rewards logic here later if we decide to separate routes.
      </p>
    </main>
  );
}
